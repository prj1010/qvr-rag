# ============================================================
# Quivr MemPalace RAG UI — requirements.txt
# Python >= 3.11 required
# Install: pip install -r requirements.txt
#          (or: uv pip install -r requirements.txt)
# ============================================================

# --- Web server ---
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.20,<1
authlib>=1.3,<2
itsdangerous>=2.2,<3

# --- LangChain core & providers ---
langchain-core>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langchain-groq>=0.3.5,<1
langchain-nvidia-ai-endpoints>=0.3.19,<1

# --- Quivr RAG core ---
quivr-core>=0.0.33,<0.1

# --- MemPalace long-term memory ---
quivr-mempalace>=0.1.0

# --- Microsoft Agent Governance Toolkit (project fork, pinned for reproducible builds) ---
# The repository is a monorepo; each direct reference must target its package
# subdirectory. The pinned commit is prj1010/agent-governance-toolkit main.
# The full core bundle is enabled on Linux/Render; RAG governance works on Windows.
agent-governance-toolkit-core[full] @ git+https://github.com/prj1010/agent-governance-toolkit.git@359a2332f57d9000924baba269ed24e4e15ad8b0#subdirectory=agent-governance-python/agent-governance-toolkit-core ; sys_platform == "linux"
agent-rag-governance @ git+https://github.com/prj1010/agent-governance-toolkit.git@359a2332f57d9000924baba269ed24e4e15ad8b0#subdirectory=agent-governance-python/agent-rag-governance

# --- Voice / TTS (Kokoro) ---
kokoro-onnx>=0.6.1,<0.7
soundfile>=0.12,<1
numpy>=1.26,<3

# --- Document parsing ---
# Install only the format-specific MarkItDown extras used by this UI.
markitdown[pdf,docx]>=0.1,<1

# --- Low-memory vector storage ---
# SQLite-backed vectors replace Quivr's in-process FAISS default.
sqlite-vec>=0.1.9,<0.2

# --- Observability ---
langfuse>=2.57,<3

# --- Utilities ---
python-dotenv>=1.0
packaging>=23.2,<25
