# ============================================================
# Quivr MemPalace RAG UI — requirements.txt
# Python >= 3.11 required
# Install: pip install -r requirements.txt
#          (or: uv pip install -r requirements.txt)
# ============================================================

# --- Web server ---
pydantic>=2.8.2,<3
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.20,<1
authlib>=1.3,<2
itsdangerous>=2.2,<3

# --- Optional cloud OCR HTTP client for the explicit DocStrange fallback ---
httpx>=0.27,<1

# --- LangChain core & providers ---
langchain-core>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langchain-groq>=0.3.5,<1
langchain-nvidia-ai-endpoints>=0.3.19,<1
langchain-openai>=0.3,<0.4

# --- Quivr RAG core ---
quivr-core>=0.0.33,<0.1

# --- MemPalace long-term memory ---
quivr-mempalace>=0.1.0

# --- Microsoft Agent Governance Toolkit ---
# The unified package is a public preview; the RAG package supplies the
# documented RAGGovernor/RAGPolicy retrieval enforcement used by this app.
agent-governance-toolkit[full]==4.1.0
agent-rag-governance==5.0.0

# --- Voice / TTS (Kokoro) ---
kokoro-onnx>=0.6.1,<0.7
soundfile>=0.12,<1
numpy>=1.26,<3

# --- Document parsing ---
# Olga handles native-text PDF, DOCX, XLSX, and HTML parsing with a small Rust
# core. Scanned PDFs continue through the lazy Docling/RapidOCR fallback.
olgadoc>=0.1.3,<0.2
# Keep MarkItDown for CSV, plain text, Markdown, and parser fallback coverage.
markitdown[pdf,docx]>=0.1,<1
# Docling is loaded lazily and used for scanned PDFs. RapidOCR avoids a
# Tesseract system dependency; the upper bound protects the current API.
docling-slim[format-pdf,feat-ocr-rapidocr-onnx]>=2.121,<3
scipy>=1.6,<2
rtree>=1.3,<2
# Docling's current PDF pipeline imports these for CPU device detection and
# image preprocessing even when layout/OCR inference uses ONNX Runtime.
torch>=2.2,<3
torchvision>=0.17,<1

# --- Low-memory vector storage ---
# SQLite-backed vectors replace Quivr's in-process FAISS default.
sqlite-vec>=0.1.9,<0.2

# --- Single-user workspace memory ---
# DuckDB stores conversation turns, rolling summaries, and restore state.
duckdb>=1.1,<2

# --- Observability ---
logfire[fastapi]>=5.0,<6

# --- Utilities ---
python-dotenv>=1.0
packaging>=23.2,<25
